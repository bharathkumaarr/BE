const wrapperExplorer = require('./wrapperExplorer');

console.log(`in wrapperDemo.js file`);

console.log(__filename, 'file-name in demo')
console.log(__dirname, 'directory-name in demo')

wrapperExplorer.greet('bharathkumar')